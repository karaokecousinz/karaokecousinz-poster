services:
  - type: web
    name: karaokecousinz-poster
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn karaokecousinz_poster:app
    envVars:
      - key: YOUTUBE_API_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false
